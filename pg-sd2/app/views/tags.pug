//- app/views/tags.pug
extends layout

block content
  .page-head
    h1 Tags
    p.meta Filter posts using tags.

  .tag-row.tag-cloud
    each t in tags
      a.tag(href=`/tags/${t.tag_id}`) ##{t.name}
        span.tag-count #{t.post_count}
