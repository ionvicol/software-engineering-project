//- app/views/500.pug
extends layout

block content
  .card.empty-state
    h1 Server error
    p Something went wrong while loading the page.
    if error
      p.meta= error
    a.button.button-accent(href="/") Return home
